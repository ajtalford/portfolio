$('.typed').typeIt({
    speed: 100,
    lifeLike: true,
    autoStart: false,
    loop: true,
    loopDelay: 0
  })
  .tiType('Web Developer')
  .tiPause(3000)
  .tiDelete()
  .tiType('Designer')
  .tiPause(3000)
  .tiDelete()
  .tiType('Coder')
  .tiPause(3000)
  .tiDelete()
  .tiType('DevOps Engineer')
  .tiPause(3000)
  .tiDelete()
  .tiType('Gamer')
  .tiPause(3000)
  .tiDelete()